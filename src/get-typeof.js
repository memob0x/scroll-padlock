/**
 * Just a typeof operator wrapper
 * @param {any} variable The variable which type needs to be retrieved
 * @returns {String} The given variable type
 */
export default (variable) => typeof variable;
