import * as Scroll from "../src/body-scroll.scroll.mjs";

const div = document.createElement("div");
div.style.height = "9999px";

beforeEach(() => document.body.append(div));

afterEach(() => div.remove());

describe("Api provider module", () => {
    // TODO:
    /*it('"isLocked" method ...', done => {
        done();
    });

    // TODO:
    it('"lock" method ...', done => {
        done();
    });

    // TODO:
    it('"unlock" method ...', done => {
        done();
    });

    // TODO:
    it('"update" method ...', done => {
        done();
    });

    // TODO:
    it("resize ...", done => {
        done();
    });*/
});
